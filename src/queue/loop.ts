// const LOOP_MODES = ['none', 'off', 'song', 'queue'] as const;
// type LOOP_MODES = typeof LOOP_MODES[number];
export enum LOOP_MODES {
    'NONE' = 0,
    'OFF' = 0,
    'SONG' = 1,
    'QUEUE' = 2
}
